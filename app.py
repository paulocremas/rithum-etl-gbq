from modules.authorization.refreshAccessToken import refreshAccessToken
from modules.extraction.extractData import extractData


if __name__ == "__main__":

    refreshAccessToken()
    extractData()
